server:
  port: 7775
  tomcat:
    uri-encoding: utf-8
spring:
  application:
    name: cyb
  rabbitmq:
    host:127.0.0.1
    port:5672
    username:guest
    password:guest
  cloud:
    config:
      uri: http://localhost:7777